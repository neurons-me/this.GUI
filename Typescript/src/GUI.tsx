/**
 * @deprecated Import from `this.gui/legacy` for compatibility-only browser/global behavior.
 * New development should use the React-first runtime from `this.gui`.
 */
export { default } from './legacy';
export * from './legacy';
