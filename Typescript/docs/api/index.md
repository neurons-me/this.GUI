# API Reference

> Generated by TypeDoc. Run `npm run docs:api` to regenerate.

## Modules

- **GUI** — main component runtime
- **Registry** — component registry and DSL
- **Router** — client-side routing primitives
- **core** — low-level GUI primitives
- **react** — React-specific exports
- **runtime** — runtime lifecycle and boot
